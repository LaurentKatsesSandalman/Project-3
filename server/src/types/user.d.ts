//Typing for user

export interface User {
    id: number;
    email: string;
}

export interface UserInput {
    email: string;
    password: string;
}

export interface completeUser {
    id: number;
    email: string;
    password: string;
}
